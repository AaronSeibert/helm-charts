apiVersion: v1
kind: Service
metadata:
  name: {{ include "postresql.fullname" . }}
  labels:
    {{- include "postresql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postresql.selectorLabels" . | nindent 4 }}

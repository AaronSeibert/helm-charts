apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bitwarden-secrets.fullname" . }}-test-connection"
  labels:
    {{- include "bitwarden-secrets.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bitwarden-secrets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
